---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-01-23T00:00:00.000Z
provider: 
---

# LiteTrans - 截图即译桌面应用

基于 **Electron + Supabase Edge Function + 百度图片翻译 API** 的 macOS 桌面工具。

**当前状态**: 核心功能完成 (95%)，可通过 `npm run dev` 本地使用。

---

## 1. 产品定义

用户通过快捷键唤起截图，自动识别并翻译截图中的文字，以"图文对照"形式展示结果。

**核心理念**: 极简交互、云端算力、即用即走。

---

## 2. 技术架构

### 2.1 技术栈

| 层 | 技术 |
|---|------|
| 框架 | Electron v28+ (electron-vite 构建) |
| 语言 | TypeScript |
| 前端 | React + TailwindCSS |
| 后端 | Supabase (Auth + PostgreSQL + Edge Functions + Storage) |
| API | 百度图片翻译 (通过 Edge Function 代理) |

### 2.2 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    渲染进程 (React)                      │
│  src/App.tsx                                            │
│  ├── 登录视图 (邮箱/密码 + OAuth GitHub)                │
│  ├── 主视图 (配额显示 + 翻译结果 + 图片预览)            │
│  ├── 个人中心 (用户信息 + 配额 + 设置 + 登出)           │
│  └── 历史记录 (列表 + 详情)                             │
└────────────────────────┬────────────────────────────────┘
                         │ IPC (preload.ts)
┌────────────────────────▼────────────────────────────────┐
│                    主进程 (Electron)                     │
│  electron/main.ts                                       │
│  ├── 全局快捷键 (可配置，默认 Alt+Q)                    │
│  ├── 系统截图 (screencapture -i)                        │
│  ├── 系统托盘 (含图标)                                  │
│  ├── 预览窗口 (独立窗口，自适应尺寸)                    │
│  ├── 复制图片到剪贴板 (nativeImage)                     │
│  └── OAuth 回调处理 (litetrans:// 协议)                 │
└─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    Supabase 后端                         │
│  ├── Auth: 用户认证 (邮箱 + OAuth GitHub)               │
│  ├── Edge Function: translate-image (代理百度 API)      │
│  ├── Storage: translation-images (历史图片存储)         │
│  ├── user_profiles: 用户配额管理                        │
│  └── RPC: check_and_use_quota / get_user_quota          │
└─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    百度图片翻译 API                       │
│  https://fanyi-api.baidu.com/api/trans/sdk/picture      │
└─────────────────────────────────────────────────────────┘
```

### 2.3 翻译流程

```
用户截图 (Alt+Q 或点击"重新截图")
    │
    ▼
检查配额 (RPC: check_and_use_quota)
    │
    ├─ 无配额 → 显示"配额已用完"
    │
    └─ 有配额 → 主进程调用 screencapture -i
                    │
                    ▼
              截图完成 → IPC 发送 base64 图片
                    │
                    ▼
              调用 Edge Function (携带 accessToken)
                    │
                    ▼
              Edge Function 验证身份 → 调用百度 API
                    │
                    ▼
              返回翻译后的图片 (base64)
                    │
                    ▼
              渲染进程显示结果 + 刷新配额
                    │
                    ▼
              上传图片到 Storage + 保存历史记录
```

---

## 3. 数据库设计 (Supabase)

### 3.1 表结构

**user_profiles** (用户配额)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键，关联 auth.users |
| plan | TEXT | 套餐: free/pro/enterprise |
| daily_limit | INTEGER | 每日配额上限 (free=20) |
| daily_used | INTEGER | 今日已用次数 |
| last_used_date | DATE | 最后使用日期 (用于每日重置) |
| expire_at | TIMESTAMPTZ | 套餐过期时间 |

**translation_history** (翻译历史)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| user_id | UUID | 关联 auth.users |
| source_lang | TEXT | 源语言 |
| target_lang | TEXT | 目标语言 |
| source_text | TEXT | 原文摘要 |
| translated_text | TEXT | 译文摘要 |
| image_path | TEXT | Storage 图片路径 |
| image_size | INTEGER | 图片大小 |
| status | TEXT | success/failed |
| error_message | TEXT | 错误信息 |

### 3.2 数据库函数

| 函数 | 说明 |
|------|------|
| `check_and_use_quota()` | 检查并扣减配额（原子操作，跨日自动重置） |
| `get_user_quota()` | 获取当前配额信息 |
| `handle_new_user()` | 新用户自动创建 profile（触发器） |

### 3.3 Storage

**translation-images** - 翻译结果图片存储
- 路径格式: `{user_id}/{timestamp}.png`
- 访问方式: 签名 URL (有效期 1 小时)

### 3.4 Edge Function

**translate-image** - 图片翻译代理
- 路径: `supabase/functions/translate-image/index.ts`
- 功能: 验证用户身份 → 调用百度 API → 返回翻译结果
- Secrets: `BAIDU_APPID`, `BAIDU_SECRET`

---

## 4. 文件结构

```
LiteTrans/
├── build/
│   ├── icon.icns            # 应用图标
│   └── trayIcon.png         # 托盘图标 (16x16)
├── electron/
│   ├── main.ts              # 主进程: 窗口/快捷键/托盘/OAuth/截图/预览
│   └── preload.ts           # IPC 桥接 (contextBridge)
├── src/
│   ├── App.tsx              # 主界面: login/main/profile/history/historyDetail
│   ├── main.tsx             # React 入口
│   ├── index.html           # HTML 模板
│   ├── index.css            # TailwindCSS 入口
│   ├── vite-env.d.ts        # Vite 类型声明
│   ├── hooks/
│   │   └── useAuth.ts       # 认证 Hook (session/quota/OAuth)
│   └── lib/
│       └── supabase.ts      # Supabase 客户端 + API 封装
├── supabase/
│   └── functions/
│       └── translate-image/
│           ├── index.ts     # Edge Function: 百度 API 代理
│           └── README.md    # Edge Function 文档
├── scripts/
│   └── test-api.ts          # API 测试脚本
├── docs/
│   ├── ROADMAP.md           # 开发计划
│   ├── TROUBLESHOOTING.md   # 问题排查指南
│   └── electron-vite-migration.md
├── .env                     # 环境变量 (Supabase URL/Key)
├── electron.vite.config.ts  # electron-vite 配置
├── tailwind.config.js       # TailwindCSS 配置
├── tsconfig.json            # TypeScript 配置
├── tsconfig.node.json       # Node.js TypeScript 配置
└── package.json
```

---

## 5. 核心模块说明

### 5.1 electron/main.ts

主进程核心功能：
- `createWindow()` - 创建无边框透明窗口 (420x500)
- `createTray()` - 系统托盘 (含图标和菜单)
- `registerShortcuts()` - 全局快捷键注册 (可配置)
- `updateShortcut()` - 动态更新快捷键
- `captureScreen()` - macOS 截图 (screencapture -i)
- `app.setAsDefaultProtocolClient('litetrans')` - OAuth 回调协议
- `previewWindow` - 独立预览窗口 (自适应图片尺寸)
- `nativeImage` + `clipboard` - 复制图片到剪贴板

**快捷键配置**:
- 预设: `Alt+Q`, `Alt+T`, `Alt+S`, `Ctrl+Shift+T`, `Ctrl+Shift+S`
- 配置持久化: `app.getPath('userData')/config.json`

### 5.2 electron/preload.ts

IPC 桥接 API (contextBridge.exposeInMainWorld)：
- `onScreenshotCaptured(callback)` - 接收截图 base64
- `onTranslateError(callback)` - 接收错误信息
- `captureScreen()` - 触发截图
- `copyImage(base64)` - 复制图片到剪贴板
- `closeWindow()` / `minimizeWindow()` / `maximizeWindow()` - 窗口控制
- `openExternal(url)` - 打开外部链接 (OAuth)
- `onOAuthCallback(callback)` - OAuth 回调
- `getShortcut()` / `setShortcut()` / `getPresetShortcuts()` - 快捷键管理
- `openPreview(base64)` - 打开预览窗口
- `toggleAlwaysOnTop()` / `getAlwaysOnTop()` - 窗口置顶控制

**关键**: 每个监听器注册前先 `removeAllListeners` 防止重复注册。

### 5.3 src/hooks/useAuth.ts

认证状态管理 Hook：
- `user` / `session` / `loading` / `quota` - 状态
- `signInWithEmail(email, password)` - 邮箱登录
- `signUpWithEmail(email, password)` - 邮箱注册
- `signInWithOAuth('github')` - GitHub OAuth
- `signOut()` - 登出 (清除 localStorage)
- `refreshQuota()` - 刷新配额

**关键**: 
- 使用 `useRef` 缓存 user，避免 `refreshQuota` 依赖循环
- OAuth 回调通过 `setTimeout` 延迟获取 quota，避免 onAuthStateChange 死锁

### 5.4 src/lib/supabase.ts

Supabase 客户端封装：
- `supabase` - 单例客户端
- `translateImageViaEdge(base64, accessToken, from, to)` - 翻译 API
- `checkAndUseQuota()` - 检查并扣减配额
- `getUserQuota()` - 获取配额信息
- `saveTranslation()` / `getTranslationHistory()` - 历史记录
- `uploadTranslationImage()` - 上传图片到 Storage
- `getTranslationImageUrl()` - 获取图片签名 URL

**关键**: `translateImageViaEdge` 接收 `accessToken` 参数，避免内部调用 `getSession()`。

### 5.5 src/App.tsx

主界面组件 (约 700 行)：
- **视图切换**: `login` / `main` / `profile` / `history` / `historyDetail`
- **登录视图**: 邮箱密码表单 + GitHub OAuth
- **主视图**: 状态指示 + 配额显示 + 翻译结果 + 操作栏 + 图片点击预览
- **个人中心**: 头像 + 用户信息 + 配额 + 快捷键设置 + 目标语言 + 登出
- **历史记录**: 列表展示 + 详情查看 (图片 + 原文 + 译文)
- **毛玻璃 UI**: `bg-glass-bg backdrop-blur-glass`
- **待翻译缓存**: `pendingImage` 状态，未登录时缓存截图

---

## 6. 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 全局快捷键截图 | ✅ | 默认 Alt+Q，可自定义 |
| 快捷键配置 | ✅ | 预设 5 种快捷键可选 |
| 百度图片翻译 | ✅ | Edge Function 代理 |
| 目标语言选择 | ✅ | 中/英/日/韩 四种语言 |
| 翻译失败重试 | ✅ | 错误时显示重试按钮 |
| 邮箱登录/注册 | ✅ | Supabase Auth |
| GitHub OAuth | ✅ | litetrans:// 协议回调 |
| 用户配额管理 | ✅ | 每日 20 次，跨日重置 |
| 配额实时显示 | ✅ | 右上角 remaining/limit |
| 复制翻译结果 | ✅ | 复制到系统剪贴板 |
| 系统托盘 | ✅ | 含图标和右键菜单 |
| 窗口置顶切换 | ✅ | 📌 按钮控制 |
| 无边框窗口 | ✅ | 自定义标题栏 |
| ESC 关闭窗口 | ✅ | 隐藏到托盘 |
| Alt+Shift+Q 显示窗口 | ✅ | 快速唤起 |
| 毛玻璃 UI | ✅ | backdrop-blur |
| 个人中心 | ✅ | 用户信息 + 配额 + 设置 |
| 翻译历史 | ✅ | 列表 + 详情查看 |
| 历史图片存储 | ✅ | Supabase Storage |
| 图片预览窗口 | ✅ | 独立窗口，自适应尺寸 |
| 预览窗口置顶 | ✅ | 独立置顶控制 |
| 未登录截图缓存 | ✅ | 登录后自动翻译 |

---

## 7. 待实现功能

详见 `docs/ROADMAP.md`

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | macOS 签名公证 | 否则 Gatekeeper 拦截 |
| P2 | 自动更新 | electron-updater |
| P2 | Windows 支持 | 当前仅 macOS |

---

## 8. 约束条件

- **必须联网**: 不支持离线使用
- **平台**: 仅 macOS (依赖 screencapture)
- **UI 风格**: 毛玻璃 (backdrop-blur)
- **登录方式**: 邮箱 + GitHub OAuth
- **配额限制**: 免费用户每日 20 次

---

## 9. 环境变量

```env
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxx...
```

Edge Function Secrets (通过 `npx supabase secrets set` 配置):
```
BAIDU_APPID=xxx
BAIDU_SECRET=xxx
```

---

## 10. 开发命令

```bash
# 开发模式
npm run dev

# 构建
npm run build

# 打包 macOS 应用
npm run electron:build

# 部署 Edge Function
npx supabase functions deploy translate-image --no-verify-jwt

# 查看 Edge Function 日志
npx supabase functions logs translate-image
```