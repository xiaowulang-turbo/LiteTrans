---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-01-20T04:11:50.924Z
provider: 
---

# LiteTrans - 截图即译桌面应用

基于 **Electron + 百度图片翻译 API + Supabase** 的跨平台桌面工具。

---

## 1. 产品定义

用户通过快捷键唤起截图，自动识别并翻译截图中的文字，以"图文对照"形式展示结果。

**核心理念**: 极简交互、云端算力、即用即走。

---

## 2. 技术架构

### 2.1 技术栈

| 层 | 技术 |
|---|------|
| 框架 | Electron v28+ |
| 语言 | TypeScript |
| 前端 | React + TailwindCSS |
| 后端 | Supabase (Auth + PostgreSQL) |
| API | 百度图片翻译 |

### 2.2 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    渲染进程 (React)                      │
│  src/App.tsx                                            │
│  ├── 登录视图 (OAuth: GitHub/Google)                    │
│  ├── 主视图 (配额显示 + 翻译结果)                        │
│  └── 设置视图 (语言切换)                                │
└────────────────────────┬────────────────────────────────┘
                         │ IPC
┌────────────────────────▼────────────────────────────────┐
│                    主进程 (Electron)                     │
│  electron/main.ts                                       │
│  ├── 全局快捷键 (Alt+Q)                                 │
│  ├── 系统截图 (screencapture / SnippingTool)           │
│  └── 百度 API 调用                                      │
└─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    Supabase 后端                         │
│  ├── Auth: 用户认证 (OAuth)                             │
│  ├── user_profiles: 用户配额                            │
│  └── translation_history: 翻译记录                      │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 数据库设计 (Supabase)

### 3.1 表结构

**user_profiles** (用户配额)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键，关联 auth.users |
| plan | TEXT | 套餐: free/pro/enterprise |
| daily_limit | INTEGER | 每日配额上限 |
| daily_used | INTEGER | 今日已用次数 |
| last_used_date | DATE | 最后使用日期 |
| expire_at | TIMESTAMPTZ | 套餐过期时间 |

**translation_history** (翻译记录)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| user_id | UUID | 用户 ID |
| source_lang | TEXT | 源语言 |
| target_lang | TEXT | 目标语言 |
| image_size | INTEGER | 图片大小 |
| status | TEXT | success/failed |
| created_at | TIMESTAMPTZ | 创建时间 |

### 3.2 数据库函数

| 函数 | 说明 |
|------|------|
| `check_and_use_quota()` | 检查并扣减配额（原子操作） |
| `get_user_quota()` | 获取当前配额信息 |
| `handle_new_user()` | 新用户自动创建 profile（触发器） |

### 3.3 RLS 策略

- 用户只能查看/更新自己的数据
- 翻译记录只能查看/插入自己的

---

## 4. 文件结构

```
LiteTrans/
├── electron/
│   ├── main.ts           # 主进程
│   ├── preload.ts        # IPC 桥接
│   ├── baidu-api.ts      # 百度翻译 API
│   └── config.ts         # 配置管理
├── src/
│   ├── App.tsx           # 主界面
│   ├── lib/
│   │   └── supabase.ts   # Supabase 客户端
│   └── hooks/
│       └── useAuth.ts    # 认证 Hook
├── .env                  # 环境变量
└── package.json
```

---

## 5. Supabase 集成方案

### 5.1 已完成

- [x] 数据库表: `user_profiles`, `translation_history`
- [x] 数据库函数: `check_and_use_quota()`, `get_user_quota()`
- [x] 触发器: 新用户自动创建 profile
- [x] RLS 策略配置
- [x] 前端 SDK: `src/lib/supabase.ts`
- [x] 认证 Hook: `src/hooks/useAuth.ts`
- [x] 依赖安装: `@supabase/supabase-js`

### 5.2 待实现

| 步骤 | 内容 | 改动量 |
|------|------|--------|
| 1 | 修复 `useAuth.ts` 依赖问题 | ~10 行 |
| 2 | 重构 `App.tsx` 集成认证 | ~100 行 |
| 3 | 主进程集成配额检查 | ~30 行 |

### 5.3 实现细节

#### Step 1: 修复 useAuth Hook

```typescript
// 问题: refreshQuota 依赖 state.user 导致无限循环
// 方案: 使用 useRef 保存 user
const userRef = useRef<User | null>(null)
userRef.current = state.user

const refreshQuota = useCallback(async () => {
  if (!userRef.current) return
  const quota = await getUserQuota()
  setState(prev => ({ ...prev, quota }))
}, []) // 无依赖
```

#### Step 2: App.tsx 重构

```
App
├── 未登录 → 显示 OAuth 登录按钮
└── 已登录
    ├── QuotaBar: 显示剩余配额 (X/20)
    ├── ResultPanel: 翻译结果
    └── 配额耗尽 → 禁用翻译，显示提示
```

#### Step 3: 翻译流程

```
截图 → 检查配额 → 有配额 → 调用百度 API → 扣减配额 → 返回结果
                → 无配额 → 返回错误
```

---

## 6. 约束条件

- **必须联网**: 不支持离线使用
- **UI 风格**: 保持现有毛玻璃风格
- **登录方式**: OAuth (GitHub/Google)
- **配额限制**: 免费用户每日 20 次

---

## 7. 环境变量

```env
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxx...
```